interface:
  display_name: "Dev Mesh Observer"
  short_description: "Diagnose current Dev Mesh coordination state"
  default_prompt: "Use $observe-dev-mesh to collect protocol 20260812.1 evidence and report active authority, contention, transaction outcomes, audit gaps, and cutover readiness."
policy:
  allow_implicit_invocation: false
