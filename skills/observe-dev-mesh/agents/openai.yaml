interface:
  display_name: "Dev Mesh Observer"
  short_description: "Inspect collaboration storylines, branches, waits, and stalls"
  default_prompt: "Use $observe-dev-mesh to collect coordination evidence and open a local console showing per-project collaboration storylines, temporary branches, waits, handoffs, and stalled-contention diagnostics."
policy:
  allow_implicit_invocation: false
